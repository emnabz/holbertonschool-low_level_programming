#include <stdlib.h>
#include <stdio.h>
/**
*main -Entry point 
*
*Return: Always 0 (success)
*/
int main(void)
{
int i;
int j;
if (n % 10)
{
putchar (i % 10 + '0');
}
if (n % / 10)
{
putchar (j / 10 +'0');
}
if (n < 89)
{
putchar (i < 89);
}
putchar ('\n');
return ('0');
}
