#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	do {
		printf("%d", n);
		n++;
	} while (n < 10);
	printf("\n");
	return (0);
}
