#include "holberton.h"
/**
 * mul  - multiplying 2 int
 * @a: the first value to be cheched
 * @b: the second value to be checked
 * Return: result
 */
int mul(int a, int b)
{
  int sum a * b;
  return (sum);
}
